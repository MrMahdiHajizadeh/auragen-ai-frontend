version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auragen_web_studio
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # - GEMINI_API_KEY=your_gemini_api_key_here
    restart: always
